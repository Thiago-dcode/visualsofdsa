import { describe, expect, it } from "vitest";
import ArrayDs from "../classes/ArrayDs";

describe("Testing Queue class", () => {
  it("Should initialize with a empty queue", () => {
    const arrayDs = new ArrayDs(10);
  });
});
